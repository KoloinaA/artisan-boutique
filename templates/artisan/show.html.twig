{% extends 'base.html.twig' %}

{% block title %}Artisan{% endblock %}

{% block body %}
    <h1>Artisan</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ artisan.id }}</td>
            </tr>
            <tr>
                <th>Business_name</th>
                <td>{{ artisan.businessName }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ artisan.description }}</td>
            </tr>
            <tr>
                <th>Profile_picture</th>
                <td>{{ artisan.profilePicture }}</td>
            </tr>
            <tr>
                <th>Created_at</th>
                <td>{{ artisan.createdAt ? artisan.createdAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>Updated_at</th>
                <td>{{ artisan.updatedAt ? artisan.updatedAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_artisan_index') }}">back to list</a>

    <a href="{{ path('app_artisan_edit', {'id': artisan.id}) }}">edit</a>

    {{ include('artisan/_delete_form.html.twig') }}
{% endblock %}
